Task 10 happy-path evidence

Shell wiring:
- `SettingsSubpage.SAFETY` now renders the real `SafetyAndLimitsScreen` inside `AppShellActivity` instead of the Task 7 placeholder.
- Settings HOME keeps the safety card as summary-only and shows the highest-risk current warning headline.

Verification commands:
1. `./gradlew :app:testDebugUnitTest --tests "*DeveloperModeDisclosureRequired*" :app:compileDebugAndroidTestKotlin`
   Result: `BUILD SUCCESSFUL`
2. `adb shell am instrument -w -e class com.opencray.app.SafetyLimitsSubpageTest org.opencray.app.test/androidx.test.runner.AndroidJUnitRunner`
   Output:
   ```
   com.opencray.app.SafetyLimitsSubpageTest:..

   Time: 8.838

   OK (2 tests)
   ```
3. `./gradlew :app:assembleDebug`
   Result: `BUILD SUCCESSFUL`

Covered assertions:
- `settingsHomeSafetySummaryUsesHighestRiskHeadline`
- `safetySubpageShowsReleaseCriticalWarningsInsideSettings`
